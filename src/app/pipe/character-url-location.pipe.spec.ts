import { CharacterUrlLocationPipe } from './character-url-location.pipe';

describe('CharacterUrlLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterUrlLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
